# Migration Guides

Migration guides are mandatory for breaking changes to contracts, schemas, or canonical rules.

Requirements:
- Each guide MUST be named using the format MIG-<ID>-<title>.md.
- Each guide MUST state the source version and target version.
- Each guide MUST include a step-by-step migration procedure and verification steps.
- Each guide MUST list impacted domains and canonical IDs.
